class Costumer extends MigrosBase {
    constructor(firstName, surName, isCard, products) {
        super (firstName, surName, isCard, products)  ;
    }

    calculate(){
        super.calculate();
        return   super.calculate();
    }
}